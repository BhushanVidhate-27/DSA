// Title: Smallest Palindromic Rearrangement II
            // Difficulty: Hard
            // Language: Python3
            // Link: https://leetcode.com/problems/smallest-palindromic-rearrangement-ii/

        if k > tot: return ""

        half = ""
        for i in range(m):
            for c in ascii_lowercase:

                tot //= j + 1
        for v in freq.values():
            for j in range(v): # tot //= factorial(v)
            tot *= i + 1

        m = len(s) >> 1
        freq = Counter(s[:m])

        tot = 1
        for i in range(m):
                if not freq[c]: continue

                t = tot * freq[c] // (m - i)
